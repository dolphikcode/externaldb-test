<%= form_with(model: article, local: true) do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nazwa %>
    <%= form.text_field :nazwa, id: :article_nazwa %>
  </div>

  <div class="field">
    <%= form.label :druga_nazwa %>
    <%= form.text_field :druga_nazwa, id: :article_druga_nazwa %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
